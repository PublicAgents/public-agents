# The reviewer bot owns the whole repository (docs/GOVERNANCE.md): every
# pull request, data or code, needs its approval and no human's. The
# colony's merge door then merges what qualifies (the reviewer's approval
# on the current head, the named checks green, never the author's own
# work). The operator keeps the repository's settings and secrets and
# can revert anything, but reviews nothing by default.
* @reviewer-public-agents-bot
