## Code change

The problem and the rule:

- [ ] no new dependency, or justified here
- [ ] refusals stay named; nothing fails silently
- [ ] `npm run validate && npm run check:schemas && npm run lint:prose && npm test && npm run typecheck`
