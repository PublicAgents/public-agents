## Tool entry

Slug:
Vendor domain (the verification host):
Provenance: <!-- vendor (with domain proof) or third-party (an unclaimed listing) -->

- [ ] `tool.json` validates against `/schemas/tool.schema.json`
- [ ] `agentAccess` says whether an agent can use it without a human account
- [ ] claims carry a source URL where the vendor makes them

Data under `registry/` is contributed under CC BY 4.0.
