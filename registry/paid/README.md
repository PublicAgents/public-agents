# Paid placements: closed

Nothing paid exists in this registry yet. This directory exists so that
a labeled paid surface, if it ever opens, is structurally separate from
measured results, case reports and claims: it can never leak into a
coverage count. CI refuses any file here (`PAID_SURFACE_CLOSED`).
